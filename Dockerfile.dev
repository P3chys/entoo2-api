FROM golang:1.23-alpine

WORKDIR /app

# Install air for hot reload (v1.61.0 works with Go 1.23)
RUN go install github.com/air-verse/air@v1.61.0

# Copy go mod files
COPY go.mod go.sum ./
RUN go mod download

# Copy source
COPY . .

EXPOSE 8000

CMD ["air", "-c", ".air.toml"]
